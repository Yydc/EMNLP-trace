# GLM-4.7-Flash — non-Qwen vendor diversity, 30B MoE
id: glm_47_flash
display_name: GLM-4.7-Flash
family: open_moe
access: local
backend: vllm
hf_repo: zai-org/GLM-4.7-Flash
quantization: awq_marlin         # AWQ-int4; fits with KV headroom
gpu_memory_utilization: 0.92
max_model_len: 32768
vllm_port: 8000
temperature: 0.2
top_p: 0.95
max_output_tokens: 4096
seed: 12345
t_max: 5
prompt_template: full_transcript
require_blame_json: true
# WARNING: dry-run shows ~5.6% parse failure rate; may need prompt-format
# tweak (see calibration_report.md after Day 0).
notes: |
  GLM uses a slightly different chat template than Qwen. Verify after
  dry_run that blame_spans JSON is parseable; if <90% parse-ok, switch
  prompt_template to glm_specific.
estimated_h100_hours_full: 1.7
estimated_api_usd_full: 0
