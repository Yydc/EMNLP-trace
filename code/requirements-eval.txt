# TraceBench evaluation-only deps (no GPU/no model serving)
# Use this when running evaluate.py / tracebench_runner.py against external APIs.
openai>=1.51.0
anthropic>=0.39.0
requests>=2.31.0
numpy>=1.26
